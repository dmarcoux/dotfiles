general {
	colors = true
	interval = 1
	output_format = 'i3bar'
}

order += cpu_usage
order += time

cpu_usage {
  format = 'CPU:%usage'
  separator_block_width = 20
}

time {
  format = '%a %d, %B %Y %T'
}
